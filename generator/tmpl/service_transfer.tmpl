{{- /*gotype: github.com/yoogoc/kratos-scaffold/generator.Service*/ -}}
package service

import (
	"fmt"
	"{{.CurrentPkgPath}}/biz"
	{{- range $i, $s := .MaybeGoPackages }}
	"{{ $s }}"
	{{- end }}
	v1 "{{.ApiPath}}"
)

{{- $genHttp := .GenHttp }}
func create{{.Name}}Req2Biz(req *v1.Create{{.Name}}Request) *biz.{{.Name}} {
	{{- range $i, $f := .CreateFields }}
		{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
		{{- if $f.FieldType.IsTime }}
			{{- if $genHttp }}
				{{- if $f.IsNillable }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := time.Unix(*{{ $reqf }}, 0)
				{{ $f.Name | ToLowCamel }} = &tmp
			}
				{{- else }}
			{{ $f.Name | ToLowCamel }} := time.Unix({{ $reqf }}, 0)
				{{- end }}
			{{- else }}
				{{- if $f.IsNillable }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := {{ $reqf }}.AsTime()
				{{ $f.Name | ToLowCamel }} = &tmp
			}
				{{- else }}
			var {{ $f.Name | ToLowCamel }} time.Time
			if {{ $reqf }} != nil {
				{{ $f.Name | ToLowCamel }} = {{ $reqf }}.AsTime()
			}
				{{- end }}
			{{- end }}
		{{- end }}
	{{- end }}
	return &biz.{{.Name}}{
		{{- range $i, $f := .CreateFields }}
			{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
			{{- if $f.FieldType.IsTime }}
				{{- $reqf = $f.Name | ToLowCamel }}
			{{- end }}
			{{ $f.Name | ToCamel }}: {{$reqf}},
		{{- end }}
	}
}

func update{{.Name}}Req2Biz(req *v1.Update{{.Name}}Request) *biz.{{.Name}} {
	{{- range $i, $f := .CreateFields }}
		{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
		{{- if $f.FieldType.IsTime }}
			{{- if $genHttp }}
				{{- if $f.IsNillable }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := time.Unix(*{{ $reqf }}, 0)
				{{ $f.Name | ToLowCamel }} = &tmp
			}
				{{- else }}
			{{ $f.Name | ToLowCamel }} := time.Unix({{ $reqf }}, 0)
				{{- end }}
			{{- else }}
				{{- if $f.IsNillable }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := {{ $reqf }}.AsTime()
				{{ $f.Name | ToLowCamel }} = &tmp
			}
				{{- else }}
			var {{ $f.Name | ToLowCamel }} time.Time
			if {{ $reqf }} != nil {
				{{ $f.Name | ToLowCamel }} = {{ $reqf }}.AsTime()
			}
				{{- end }}
			{{- end }}
		{{- end }}
	{{- end }}
	return &biz.{{.Name}}{
		{{- $reqf := printf "req.%s" (.PrimaryField.Name | ToCamel) }}
		{{ .PrimaryField.Name | ToCamel }}: {{$reqf}},
		{{- range $i, $f := .UpdateFields }}
			{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
			{{- if $f.FieldType.IsTime }}
				{{- $reqf = $f.Name | ToLowCamel }}
			{{- end }}
			{{ $f.Name | ToCamel }}: {{$reqf}},
		{{- end }}
	}
}

func patch{{.Name}}Req2Biz(req *v1.Patch{{.Name}}Request) *biz.{{.Name}}Patch {
	{{- range $i, $f := .PatchFields }}
		{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
		{{- if $f.FieldType.IsTime }}
			{{- if $genHttp }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := time.Unix(*{{ $reqf }}, 0)
				{{ $f.Name | ToLowCamel }} = &tmp
			}
			{{- else }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				tmp := {{ $reqf }}.AsTime()
				{{ $f.Name | ToLowCamel }} = &tmp
			}
			{{- end }}
		{{- end }}
	{{- end }}
	return &biz.{{.Name}}Patch{
		{{- $reqf := printf "req.%s" (.PrimaryField.Name | ToCamel) }}
		{{ .PrimaryField.Name | ToCamel }}: {{$reqf}},
		{{- range $i, $f := .PatchFields }}
			{{- $reqf := printf "req.%s" ($f.Name | ToCamel) }}
			{{- if $f.FieldType.IsTime }}
				{{- $reqf = $f.Name | ToLowCamel }}
			{{- end }}
			{{ $f.Name | ToCamel }}: {{$reqf}},
		{{- end }}
	}
}

func {{.Name | ToLowCamel}}Biz2Proto(item *biz.{{.Name}}) *v1.{{.Name}} {
	{{- range $i, $f := .TransferFields }}
		{{- $reqf := printf "item.%s" ($f.Name | ToCamel) }}
		{{- if and $f.FieldType.IsTime $f.IsNillable }}
			{{- if $genHttp }}
			{{ $f.Name | ToLowCamel }} := lo.IfF({{ $reqf }} != nil, func() *int64 {
				return lo.ToPtr({{ $reqf }}.Unix())
			}).Else(nil)
			{{- else }}
			{{ $f.Name | ToLowCamel }} := lo.IfF({{ $reqf }} != nil, func() *timestamppb.Timestamp {
				return timestamppb.New(*{{ $reqf }})
			}).Else(nil)
			{{- end }}
		{{- end }}
	{{- end }}
	return &v1.{{.Name}}{
		{{- range $i, $f := .TransferFields }}
			{{- $reqf := printf "item.%s" ($f.Name | ToCamel) }}
			{{- if $f.FieldType.IsTime }}
				{{- if $f.IsNillable }}
				{{- $reqf = $f.Name | ToLowCamel }}
				{{- else }}
				{{- $reqf = printf ($f.FieldType.Biz2ProtoByHttp $genHttp) $reqf }}
				{{- end }}
			{{- end }}
			{{ $f.Name | ToCamel }}: {{$reqf}},
		{{- end }}
	}
}

func bulk{{.Name}}Biz2Proto(list []*biz.{{.Name}}) []*v1.{{.Name}} {
	var target []*v1.{{.Name}}
	for _, item := range list {
		target = append(target, {{.Name | ToLowCamel}}Biz2Proto(item))
	}
	return target
}

func param{{.Name}}Proto2Biz(req *v1.{{.Name}}Param) *biz.{{.Name}}Param {
	if req == nil {
		return &biz.{{.Name}}Param{}
	}
	{{- range $i, $f := .Fields.ParamFields }}
		{{- if and $f.FieldType.IsTime (not $f.Type.IsIn) }}
			{{- $reqf := printf "req.%s%s" ($f.SourceName | ToCamel) $f.Type.StringProto }}
			{{- if $genHttp }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				item := time.Unix(*{{$reqf}}, 0)
				{{ $f.Name | ToLowCamel }} = &item
			}
			{{- else }}
			var {{ $f.Name | ToLowCamel }} *time.Time
			if {{ $reqf }} != nil {
				item := {{$reqf}}.AsTime()
				{{ $f.Name | ToLowCamel }} = &item
			}
			{{- end }}
		{{- end }}
	{{- end }}

	{{- range $i, $f := .Fields.ParamFields }}
		{{- $reqf := printf "req.%s%s" ($f.SourceName | ToCamel) $f.Type.StringProto }}
		{{- if and $f.FieldType.IsTime $f.Type.IsIn }}
			{{- if $genHttp }}
			{{ $f.Name | ToLowCamel }} := make([]time.Time, 0, len({{$reqf}}))
			for _, ts := range {{$reqf}} {
				{{ $f.Name | ToLowCamel }} = append({{ $f.Name | ToLowCamel }}, time.Unix(ts, 0))
			}
			{{- else }}
			{{ $f.Name | ToLowCamel }} := make([]time.Time, 0, len({{$reqf}}))
			for _, ts := range {{$reqf}} {
				{{ $f.Name | ToLowCamel }} = append({{ $f.Name | ToLowCamel }}, ts.AsTime())
			}
			{{- end }}
		{{- end }}
	{{- end }}

	return &biz.{{.Name}}Param{
		{{- range $i, $f := .Fields.ParamFields }}
			{{- $reqf := printf "req.%s%s" ($f.SourceName | ToCamel) $f.Type.StringProto }}
			{{- if $f.FieldType.IsTime }}
				{{ $f.Name | ToCamel }}: {{$f.Name | ToLowCamel }},
			{{- else }}
				{{ $f.Name | ToCamel }}: {{$reqf}},
			{{- end }}
		{{- end }}
	}
}
